# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project specific
tmp/
test_assets/
examples/output/
*.svg
!docs/examples/*.svg
!docs/demo/*.svg
!docs/journey/svg/*.svg
# Packaged SVG templates are source, not experiment output. The blanket rule
# above silently swallowed all 19 of them when vector markup moved out of
# Python and into templates: `git add` obeys .gitignore without complaining,
# so they were only ever present in working trees. Every fresh checkout —
# including any CI-built wheel — shipped without them.
!src/splatthis/templates/svg/*.svg

# Local Claude Code settings
.claude/

# Cloned reference repos (kept locally, never committed)
/external/
/ml-sharp/

# Experiment artifacts (kept locally)
/artifacts_*/
/fidelity_lab_*/
/tmp_profile*

# Root-level scratch policy: experiment outputs and one-off scripts at the
# repo root are never tracked. The canonical CLI lives in src/splatthis/cli.py
# and is installed as the `splatthis` entry point; durable scripts belong in
# scripts/ or tools/, durable outputs in docs/.
/*.py
/*.html
/*.json
/*.png
/*.js

# Whitelisted root files
!/pyproject.toml

# Note: /*.svg is already covered by the global *.svg rule above.

# PowerPoint owner-lock files, created whenever a deck is opened.
~$*

# Measured experiment outputs. The curated result set is tracked; the two
# heavy regenerable artifacts are not (see result/README.md).
/result/artifacts/chameleon.svg
/result/artifacts/powerpoint_screen_capture.png

# Reference corpus: images regenerate from skimage.data via
# `python tools/corpus_benchmark.py --materialize`; runs are large.
# corpus.json (the manifest with hashes) and results.jsonl ARE tracked.
/result/corpus/images/
/result/corpus/runs/
/result/corpus/index.html
